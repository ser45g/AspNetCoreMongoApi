apiVersion: v1
kind: Service
metadata:
  name: weather-forecast-service
spec:
  type: NodePort
  selector:
    app: weather-forecast
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    nodePort: 30018
